# Express API base URL (no trailing slash). Used server-side by the onboarding route.
REVORA_BACKEND_URL=http://localhost:8080
# Public API URL (no trailing slash). Required for browser authentication and the admin dashboard.
NEXT_PUBLIC_REVORA_API_URL=http://localhost:8080
